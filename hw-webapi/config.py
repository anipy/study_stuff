"""Some default configs for app"""

DB = "db.db"

DEFAULT_QUOTE = (
    "The problem with quotes on the Internet is "
    "that people immediately believe in their authenticity."
)

DEFAULT_IMAGE = "default.jpg"
